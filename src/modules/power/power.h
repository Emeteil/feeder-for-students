#pragma once

/*
 * Power - мониторинг системы питания (напряжение батареи и т.п.).
 *
 * Владелец модуля: команда "система питания" (см. обсуждение в чате -
 * отдельная почти независимая задача).
 *
 * Пишет State.batteryVoltage. Другие модули (например Display) могут
 * читать это поле напрямую, чтобы показать иконку заряда.
 */
namespace Power
{
    void Init();
    void Update();
}
