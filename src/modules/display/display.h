#pragma once

/*
 * Display - сенсорный экран: отрисовка UI, обработка нажатий, QR-код.
 *
 * Владелец модуля: команда "frontend / UX".
 *
 * Логика взаимодействия с Dispenser ещё не определена.
 */
namespace Display
{
    void Init();
    void Update();
}
