#pragma once

/*
 * App - точка сборки прошивки. Здесь и только здесь перечисляются все
 * модули проекта. Сам .ino-файл ничего не знает про модули, он просто
 * вызывает App::Init()/App::Update().
 *
 * Добавили новый модуль в src/modules/? Добавьте его Init()/Update()
 * в app.cpp - это единственное место, которое нужно трогать, чтобы
 * подключить модуль к прошивке.
 */
namespace App
{
    // Вызывается один раз из setup().
    void Init();

    // Вызывается на каждой итерации из loop().
    // НЕ должна содержать delay() или другой блокирующий код -
    // см. docs/architecture.md, раздел "Блокирующие операции".
    void Update();
}
