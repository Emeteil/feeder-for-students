<!--
  Шаблон документации модуля.
  Скопируйте этот файл в docs/modules/<name>.md (name — как папка в
  src/modules/) и заполните. Удалите эту подсказку и комментарии-подсказки
  в фигурных скобках после заполнения.
-->

# Модуль: `<name>`

Папка в коде: `src/modules/<name>/`

## Что делает

{1-3 предложения: за что отвечает модуль, простыми словами}

## Публичный API

{Обычно это просто Init()/Update() — но если есть что-то ещё важное,
опишите здесь}

## События

**Публикует:**
- `EventType::...` — когда и что значит `intValue`

**Слушает:**
- `EventType::...` — что делает при получении

## Поля AppState

{Какие поля AppState этот модуль пишет / читает}

## Пины и железо

{Какие PIN_... из src/core/pins.h использует, какая физическая деталь
к ним подключена}

## Библиотеки

{Внешние библиотеки: автор/имя, версия, зачем нужна}

## Известные проблемы / TODO

{Что не доделано, на что обратить внимание следующему, кто будет
трогать модуль}
